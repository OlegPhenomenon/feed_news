<%= button_to url, method: @method, class: 'btn btn-primary px-3 me-1',
                                                  form: @method == :delete ? { data: { turbo_confirm: "Are you sure?" } } : {},
                                                  style: "color: white; background-color: #{@color};" do %>
  <%= content %>
<% end %>
